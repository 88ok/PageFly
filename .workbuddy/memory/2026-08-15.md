# 2026-08-15 工作日志

## 分享页头部精简（render.js）
- 移除页面类型徽章 `.type-badge`：电脑端与手机端都不再展示 HTML/Markdown 类型。
- 手机端（≤640px）仅隐藏标题 `.vtitle`，保留 logo 与右侧「创建页面」「复制链接」按钮。
- 电脑端标题仍正常展示。
- commit `403b56c`，已 push 到 origin/main。
- 用户偏好：手机端保留 logo，不要连同标题一起去掉；UI 空间不足时优先砍非必要信息（标题/类型），保留功能按钮与品牌标识。
